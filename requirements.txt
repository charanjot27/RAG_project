# Core RAG + deep-learning stack (see DOCUMENTATION.md §6).
# Versions are lower-bounds known to work together; run `pip freeze > requirements.txt`
# to pin exact versions once your environment is stable.

sentence-transformers>=3.0
transformers>=4.44
torch>=2.2
qdrant-client>=1.11
rank-bm25>=0.2.2
ddgs>=6.0            # keyless web search (web retrieval mode)
trafilatura>=1.8     # clean main-text extraction from web pages
ragas>=0.1.21
datasets>=2.19
fastapi>=0.111
uvicorn[standard]>=0.30
streamlit>=1.36
pypdf>=4.2
python-dotenv>=1.0
groq>=0.11
anthropic>=0.34
requests>=2.31
